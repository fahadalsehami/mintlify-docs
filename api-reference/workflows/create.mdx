---
title: "POST /api/agent-workflows"
description: "Create an agent workflow"
---

## `POST /api/agent-workflows`

Create an agent workflow definition.

### Example

```bash
curl -X POST https://api.medera.ai/api/agent-workflows \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json"
```
