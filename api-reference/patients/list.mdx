---
title: "GET /api/patients"
description: "List patients"
---

```

GET /api/patients

```

Lists tenant patients (RLS-enforced). Cursor-paginated; supports filters: `name`, `dob`, `provider_id`, `program`.
