---
title: "PATCH /api/patients/{id}"
description: "Update a patient"
---

```text

PATCH /api/patients/{patientId}

```

Partial update. PHI changes generate a separate `phi_audit_logs` entry per field.
