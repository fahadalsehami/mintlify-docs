---
title: "POST /api/prior-auth/requests/{id}/appeal"
description: "Generate an appeal"
---


```
POST /api/prior-auth/requests/{request_id}/appeal
```

Transitions the workflow to `PA_APPEAL_DRAFT` and generates the appeal letter with new evidence (PubMed Expert + chart context). Submission via the original channel.
