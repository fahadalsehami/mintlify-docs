---
title: "Refresh a session JWT"
description: "Use Clerk's session refresh"
---


Clerk handles JWT refresh transparently on the client. There is no Medera-issued refresh token.

If you cache JWTs server-side (proxy pattern), refresh via:

```javascript
const newJwt = await getToken({ template: 'medera-api', skipCache: true });
```
