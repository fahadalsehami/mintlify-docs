---
title: "API Versioning"
description: "How the API is versioned and changes are managed"
---

The Medera API uses URL-based versioning. The current major is `v1` and is implicit in the path.

Breaking changes follow the [Change Policy](/release-notes/change-policy) — 90 days notice minimum, deprecation headers, and a parallel new major path.
