---
title: "Note Templates"
description: "Working with Medera templates"
--- Templates are persisted in `note_templates` (backend) and exposed through both the backend (for tenant-managed templates) and AI Services (for system templates). Template versioning is semver; deprecated templates retain their last published version for trailing executions.